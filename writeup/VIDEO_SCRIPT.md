# ConsentGuard — 5-Minute Video Script (v3 — technical UX/DX cut)

Target runtime **4:40** (buffer under 5:00). Screen-first: judges grade the working
product. The Gatekeeper animation survives as ~17s of accents (hook + close);
everything else is real screen capture. Narration: Sienna (8 segments, ~3:35).

Evaluation mapping (video criterion): problem ✓ S2 · why agents ✓ S3 · architecture ✓ S4 · demo ✓ S5 (UX) · the build ✓ S5b+S6 (DX) · Antigravity ✓ S6 · deployability ✓ S6.

| # | Time | Shot (what's on screen) | VO |
|---|---|---|---|
| S1 | 0:00–0:15 | **Cold open — UX.** Screen: ADK playground. Type "Review campaign_summer.json". Verdict table streams in: 3 PASS / 5 BLOCK. On the word "no": 3s insert of the gauntlet-slam animation (B1), hard cut back. | VO-1 (14.1s): "Every business just hired an AI agent that can send a thousand messages an hour. Here's the question nobody's asking: who told it it was allowed to? This is ConsentGuard — the agent that tells other agents no." |
| S2 | 0:15–0:45 | **Problem — engineering framing.** Screen: code editor showing a generic outreach agent with a bare `send_email()` tool — zoom on the missing authorization layer. Overlay stat cards: $10M CASL · $53k CAN-SPAM · $1.5k TCPA; consent-window timeline graphic. Optional 3s foundry accent (B2). | VO-2 (29.4s): "Outreach agents are fast, tireless — and blind. Consent law isn't a checkbox. In Canada, consent is channel-specific — email consent doesn't cover SMS — and implied consent expires: two years after a purchase, six months after an inquiry. Penalties run to ten million dollars per violation. Everyone's building agents that press Send. Almost no one is building the layer that decides whether Send is allowed. We built the brakes." |
| S3 | 0:45–1:13 | **Why agents.** Screen: `data/seed_contacts.json` scrolling — c_004's messy history (withdrawal → renewed express). Then template_b missing its unsubscribe. Split-screen: "missed violation = fines" / "false positive = lost revenue". | VO-3 (27.5s): "Why agents, and not an if-statement? Because evidence is messy — withdrawals followed by renewed opt-ins, form wording that may or may not be express consent — and message content needs semantic review. And because auditors err in both directions: missed violations cost fines; false positives cost revenue. Judgments like these need specialized agents. The final decision needs the opposite." |
| S4 | 1:13–1:46 | **Architecture.** Screen: the mermaid diagram from README (animated highlight per node as named). Then cut to `app/gate.py` — highlight the asymmetry: `allowed = False  # agents may tighten the gate — never open it`, and the signature showing the gate re-pulls ledger facts. Land the key-turn animation (B5, 4s) on "nothing an agent says can open it". | VO-4 (33.2s): "ConsentGuard is a multi-agent system on Google's Agent Development Kit. An orchestrator loads the campaign. A consent auditor — the producer — pulls evidence from our Consent Ledger, a custom MCP server, and flags problems. An adversarial verifier independently re-pulls the evidence and adjudicates every flag — both ways. And then: a deterministic gate. Pure code. Fail-closed. Here's the security invariant the whole system rests on: agents advise, code decides. Agent findings can tighten the gate. Nothing an agent says can open it. A prompt-injected agent gains exactly nothing." |
| S5 | 1:46–2:35 | **UX demo (the judged core).** Screen recording, full run in playground: auditor's per-recipient report → verifier adjudication turn — zoom on c_004: flag raised, history pulled, "latest record governs", flag OVERTURNED → gate verdicts with plain-English reasons → `tail data/audit_log.jsonl`. Callout boxes on each block reason (expired window, channel mismatch, missing unsubscribe). | VO-5 (45.5s): "Let's run it. Driftwood Coffee Roasters — fictional — queues a summer promo to eight contacts. ConsentGuard passes three and blocks five, each with cited evidence. Chloe inquired seven months ago — the six-month window expired: blocked. Hana consented on SMS, this is email — channel mismatch: blocked. Grace's template lost its unsubscribe link: blocked, even with valid consent. And here's my favorite: Dev unsubscribed last year — the auditor flags him. But the verifier pulls his full history, finds he re-subscribed in May, and overturns the false positive. The gate confirms against facts and releases the send. Fines prevented, and revenue protected. Every decision lands in an append-only, pseudonymous audit log — regulator-ready." |
| S5b | 2:35–2:55 | **DX bridge.** Screen: `uv run pytest` → 9/9 green, hold on `test_agent_opinion_cannot_open_gate`. Then `skills/` folder: open both SKILL.md rule packs side-by-side — swap = drop-in file. | VO-8 (~18s): "That's the user experience: paste a campaign, get verdicts with cited evidence. The developer experience is just as deliberate. The gate's invariants are unit-tested. Jurisdictions are drop-in skill files. And the ledger speaks MCP — so any client, in any framework, can query the same consent evidence." |
| S6 | 2:55–3:35 | **DX deep cuts.** Fast sequence: ① Antigravity generating the CAN-SPAM pack from the CASL template ② `agents-cli eval` run vs `eval/golden_scenarios.json` — verdict exact-match + LLM-as-judge ③ the Consent Ledger MCP server attached to Gemini CLI answering "does c_003 have valid email consent?" ④ `agents-cli scaffold enhance` → Terraform → Cloud Run console with the deployed service. | VO-6 (38.0s): "The build: spec-first. The gate and its nine invariant tests existed before any agent did — including the test that no agent opinion can ever open the gate. Jurisdiction rules ship as pluggable agent skills — here's Antigravity generating the US CAN-SPAM pack from the Canadian template. The ledger is a standard MCP server — same consent data, answering questions in Gemini CLI. Evaluated against a golden scenario set with agents-cli, and deployed to Cloud Run with the scaffolded Terraform." |
| S7 | 3:35–3:50 | **Close.** 4s dawn animation (B6) → end card: repo URL + "agents advise. code decides." Hold 4s. | VO-7 (9.4s): "Everyone's building agents that can act. The interesting problem is deciding when they're allowed to. ConsentGuard is open source — link below. Thanks for watching." |

Skeleton total ≈ 3:50; recording pauses and breathing room bring the final cut to ≈ 4:30.

## Production notes

- **Record at 1080p+, zoom in post** — terminal fonts ≥ 16pt; judges watch on laptops.
- The c_004 overturn is the money moment — give the verifier's reasoning a full-screen beat.
- All on-screen text (stats, callouts, labels) is editor overlay — never in generated media.
- Animation inserts total ~17s (B1 3s, B2 3s optional, B5 4s, B6 4s) — cut them first if over time; B3/B4 unused in v3 (library spares).
- Disclose in the YouTube description: b-roll accents + narration are AI-generated (Higgsfield via MCP) — itself a demo of agent-driven media tooling.
- Timed read before editing; hard cap 5:00.
